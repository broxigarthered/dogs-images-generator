import XCTest
@testable import dogs_images_generator

final class dogs_images_generatorTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(dogs_images_generator().text, "Hello, World!")
    }
}
